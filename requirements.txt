# Dependencies are added phase by phase.

# Phase 1 — data collection
requests==2.34.2
pandas==3.0.3

# Phase 2 — modeling
scipy==1.18.0
pytest==9.1.1

# Phase 3 — backtest report
matplotlib==3.11.0

# Phase 4 — backend
fastapi==0.128.3
uvicorn==0.40.0
python-dotenv==1.2.1
