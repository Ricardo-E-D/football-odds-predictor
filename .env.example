# Copy to .env and fill in. The real .env is gitignored — never commit keys.
ODDS_API_KEY=your-key-from-the-odds-api.com
# Optional: protects POST /api/refresh (any secret string)
REFRESH_TOKEN=
# Optional: odds cache lifetime, default 12
ODDS_CACHE_TTL_HOURS=12
